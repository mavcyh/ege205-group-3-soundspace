import { Metadata } from "next";
import React from 'react'

export const metadata: Metadata = {
    title: "SoundSpace | Admin Password",
  };

export default function AdminPassword() {
    return(
        <>
            <p>this is admin pass page</p>
        </>
    );
}